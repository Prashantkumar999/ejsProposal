<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Property Management Proposal</title>

  <!-- Geist Font -->
  <link href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    body {
      background: #f4f4f4;
      display: flex;
      justify-content: center;
      font-family: 'Geist', system-ui, sans-serif;
    }

    /* PAGE SIZE — MATCHING BROCHURE */
    .a4-page {
      width: 491px;
      height: 694px;
      background: #fff;
      padding: 20px;
      box-sizing: border-box;
      position: relative;
    }

    /* INNER BORDER */
    .inner-div {
      width: 451px;
      height: 654px;
      border: 2px solid #c9a26b;
      padding: 20px;
      box-sizing: border-box;
      position: relative;
      z-index: 2;
    }

    /* LOGO */
    .logo {
      text-align: center;
      margin-bottom: 22px;
    }

    .logo img {
      height: 60px;
    }

    /* TITLE */
    .title {
      text-align: center;
      color: #8b5e2f;
      font-weight: 700;
      font-size: 18px;
      line-height: 1.4;
      margin-bottom: 18px;
    }

    /* SECTION */
    .section {
      margin-bottom: 12px;
    }

    .section-title {
      text-align: center;
      font-weight: 600;
      font-size: 11px;
      margin-bottom: 12px;
      color: #222;
    }

    .label {
      font-size: 10px;
      letter-spacing: 0.5px;
      color: #8b5e2f;
      font-weight: 600;
      text-transform: uppercase;
      margin-bottom: 2px;
    }

    .value {
      font-size: 11px;
      font-weight: 600;
      color: #222;
    }

    .muted {
      font-size: 10px;
      color: #000;
      font-weight: 500;
      line-height: 1.5;
      margin-top: 18px;
    }

    /* SKYLINE */
    .skyline {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 491px;
      opacity: 0.08;
      pointer-events: none;
      z-index: 1;
    }

    /* FOOTER */
    .footer {
      position: absolute;
      bottom: 5px;
      left: 20px;
      font-size: 10px;
      color: #777;
      z-index: 3;
    }
  </style>
</head>

<body>

<div class="a4-page">

  <div class="inner-div">

    <!-- Logo -->
    <div class="logo">
      <img src="/images/royal_homes_svglogo.svg" alt="Royal Holiday Homes Logo">
    </div>

    <!-- Main Title -->
    <h1 class="title">
      PROPERTY MANAGEMENT PROPOSAL<br />
      FOR 2 BEDROOM DUPLEX APARTMENT,<br />
      EMAAR BUILDING
    </h1>

    <!-- Prepared For -->
    <div class="section">
      <div class="section-title">PREPARED EXCLUSIVELY FOR</div>
    </div>

    <!-- Details -->
    <div class="section">
      <div class="label">Owner(s)</div>
      <div class="value"><%= ownerName %></div>
    </div>

    <div class="section">
      <div class="label">Property Details</div>
      <div class="value"><%= propertyDetails %></div>
    </div>

    <div class="section">
      <div class="label">Proposal Ref#</div>
      <div class="value"><%= proposalRef %></div>
    </div>

    <div class="section">
      <div class="label">Date</div>
      <div class="value"><%= date %></div>
    </div>

    <!-- Description -->
    <p class="muted">
      Based on our analysis of comparable units in Abu Dhabi, we forecast your property to generate
      <strong><%= annualRevenue %></strong> annually at <strong><%= occupancy %> occupancy</strong>, representing a
      <strong><%= increase %> increase</strong> compared to traditional long-term leasing in the same area.
    </p>

  </div>

  <!-- Skyline -->
  <img src="/images/illustration.png" class="skyline" alt="Skyline">

  <!-- Footer -->
  <div class="footer">
    Confidential © Royal Holiday Homes
  </div>

</div>

</body>
</html>
