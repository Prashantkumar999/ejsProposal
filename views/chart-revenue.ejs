<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Expected Revenue Comparison</title>

  <!-- Geist Font -->
  <link href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700&display=swap" rel="stylesheet">

  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <style>
    *,
    *::before,
    *::after {
      font-family: 'Geist', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }

    body {
      background: #f4f4f4;
      display: flex;
      justify-content: center;
      margin: 0;
    }

    /* PAGE */
    .a4-page {
      width: 491px;
      height: 694px;
      background: #fff;
      padding: 20px;
      box-sizing: border-box;
      position: relative;
      overflow: hidden;
    }

    /* INNER BORDER */
    .inner-div {
      width: 451px;
      height: 654px;
      border: 2px solid #c9a26b;
      padding: 5px 20px;
      box-sizing: border-box;
      position: relative;
      z-index: 2;
    }

    /* LOGO */
    .corner-logo {
      width: 14px;
      position: absolute;
      top: 18px;
      right: 18px;
    }

    /* HEADINGS */
    .heading {
      text-align: center;
      color: #8b5e2f;
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 15px;
    }

    .subheading {
      text-align: center;
      font-size: 16px;
      font-weight: 500;
      color: #7A542A;
    }

    .caption {
      text-align: center;
      font-size: 12px;
      font-weight: 600;
      margin-bottom: 12px;
    }

    /* CHART */
    .chart-wrapper {
      height: 320px;
      margin-top: 70px;
    }

    canvas {
      width: 100% !important;
      height: 100% !important;
    }

    /* SKYLINE BACKGROUND */
    .skyline {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 491px;
      opacity: 0.08;
      pointer-events: none;
      z-index: 1;
    }

    /* FOOTER */
    .footer {
      position: absolute;
      bottom: 5px;
      left: 20px;
      font-size: 10px;
      color: #777;
      z-index: 3;
    }
  </style>
</head>

<body>

<div class="a4-page">

  <div class="inner-div">

    <img src="/images/svg_logo.svg" class="corner-logo" alt="Logo">

    <h1 class="heading">EXPECTED REVENUE COMPARISON</h1>
    <div class="subheading">Revenue vs Profit Analysis</div>
    <div class="caption">Gross revenue vs net income by scenario</div>

    <div class="chart-wrapper">
      <canvas id="revenueChart"></canvas>
    </div>

  </div>

  <!-- Skyline background (same as proposal page) -->
  <img src="/images/illustration.png" class="skyline" alt="Skyline">

  <div class="footer">Confidential © Royal Holiday Homes</div>

</div>

<script>
  /* Force Geist inside Chart.js */
  Chart.defaults.font.family = "Geist, system-ui, sans-serif";
  Chart.defaults.font.size = 10;

  const scenarios = ["Conservative", "Realistic", "Confident", "Optimistic"];
  const percentages = ["75%", "80%", "85%", "90%"];

  const chartData = {
    labels: scenarios.map((label, i) => [label, percentages[i]]),
    datasets: [
      {
        label: "Gross Revenue",
        data: [243430, 259330, 275850, 291740],
        backgroundColor: "#3b82f6",
        barPercentage: 0.7
      },
      {
        label: "Net Revenue",
        data: [170250, 183440, 197150, 210350],
        backgroundColor: "#b08b57",
        barPercentage: 0.7
      }
    ]
  };

  /* Value labels on bars */
  const valueLabelPlugin = {
    id: 'valueLabel',
    afterDatasetsDraw(chart) {
      const { ctx } = chart;
      ctx.save();
      ctx.font = '600 9px Geist';
      ctx.textAlign = 'center';

      chart.data.datasets.forEach((dataset, datasetIndex) => {
        chart.getDatasetMeta(datasetIndex).data.forEach((bar, i) => {
          ctx.fillStyle = datasetIndex === 1 ? '#9ca3af' : '#000'; // gray for Net Revenue
          ctx.fillText(
            (dataset.data[i] / 1000).toFixed(1) + "K",
            bar.x,
            bar.y - 6
          );
        });
      });

      ctx.restore();
    }
  };

  new Chart(document.getElementById("revenueChart"), {
    type: "bar",
    data: chartData,
    options: {
      responsive: true,
      maintainAspectRatio: false,
      plugins: {
        legend: {
          position: "bottom",
          labels: {
            font: { size: 10, weight: "600" }
          }
        }
      },
      scales: {
        x: {
          ticks: {
            color: "#000",
            font: { size: 10, weight: "600" }
          },
          grid: { display: false }
        },
        y: {
          max: 300000,
          ticks: {
            stepSize: 75000,
            callback: v => v / 1000 + "K",
            font: { size: 9 }
          }
        }
      }
    },
    plugins: [valueLabelPlugin]
  });
</script>

</body>
</html>
